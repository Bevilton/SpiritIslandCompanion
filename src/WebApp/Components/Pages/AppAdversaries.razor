@page "/app/adversaries"
@attribute [Authorize]
@inherits CatalogueReferencePage

<PageTitle>Adversaries</PageTitle>

<AdversariesView OwnedExpansions="@OwnedExpansions" Records="Records" Facts="Facts"/>

@code {
    protected override IReadOnlyDictionary<string, SetupInsights.PlayRecord> Project(
        IReadOnlyList<Application.Features.Statistics.SetupGameFact> facts) => SetupInsights.AdversaryPlayRecords(facts);
}
