@page "/app/scenarios"
@attribute [Authorize]
@inherits CatalogueReferencePage

<PageTitle>Scenarios</PageTitle>

<ScenariosView OwnedExpansions="@OwnedExpansions" Records="Records" Facts="Facts"/>

@code {
    protected override IReadOnlyDictionary<string, SetupInsights.PlayRecord> Project(
        IReadOnlyList<Application.Features.Statistics.SetupGameFact> facts) => SetupInsights.ScenarioRecords(facts);
}
